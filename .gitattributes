# Prebuilt binaries are committed directly (each below GitHub's 100 MB limit):
#   device/glodroid/dragon_q6a/prebuilt/Image  (~61 MB)
#   prebuilts-radxa/modules.tar.gz             (~45 MB)
# Ready-to-flash system images ship as Release assets, not in the tree.

# Treat these as binary (no EOL conversion, no diff).
*.img    binary
*.tar.gz binary
*.mbn    binary
*.bin    binary
*.fw     binary
*.EFI    binary
*.dtb    binary
*.tplg   binary
